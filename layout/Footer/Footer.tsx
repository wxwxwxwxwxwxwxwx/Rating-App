import classNames from "classnames";
import { IFooterProps } from "./Footer.props";
import styles from "./IFooterProps.module.css";

export const Footer = ({ ...props }: IFooterProps) => {
  return <div {...props}>Footer</div>;
};
